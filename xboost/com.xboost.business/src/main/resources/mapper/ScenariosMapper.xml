<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xboost.mapper.ScenariosMapper">

    <cache/>

    <insert id="save">
        INSERT into t_scenarios_info(user_id,scenarios_name,scenarios_category,scenarios_desc,scenarios_model,scenarios_out,
        scenarios_status,last_open_time,str1,str2,str3,createtime,updatetime)
        VALUES (#{userId},#{scenariosName},#{scenariosCategory},#{scenariosDesc},#{scenariosModel},#{scenariosOut},
        #{scenariosStatus},#{lastOpenTime},#{str1},#{str2},#{str3},#{createTime},#{updateTime})
    </insert>


    <select id="findAll" resultType="com.xboost.pojo.Scenarios">
        SELECT * FROM t_scenarios_info
    </select>

    <select id="findByParam" parameterType="map" resultType="Scenarios" resultMap="scenariosMap">
        SELECT * from t_scenarios_info
        <where>
            <if test="keyword != null and keyword != ''">
                scenarios_name LIKE #{keyword} or scenarios_category like #{keyword} or scenarios_desc like #{keyword}
            </if>
        </where>
        order by ${orderColumn} ${orderType}
        limit ${start},${length}
    </select>

    <select id="findAllCount" resultType="long">
        SELECT COUNT(*) FROM t_scenarios_info
    </select>

    <select id="findCountByParam" resultType="long" parameterType="map">
        SELECT count(*) FROM t_scenarios_info
        <where>
            <if test="keyword != null and keyword != ''">
                scenarios_name LIKE #{keyword} or scenarios_category like #{keyword} or scenarios_desc like #{keyword}
            </if>
        </where>
    </select>

    <select id="findById" resultType="com.xboost.pojo.Scenarios"  resultMap="scenariosMap">
        SELECT * FROM t_scenarios_info where id = #{id}
    </select>

    <update id="edit">
        UPDATE t_scenarios_info SET user_id = #{userId},scenarios_name = #{scenariosName},scenarios_category=#{scenariosCategory},
        scenarios_desc=#{scenariosDesc},scenarios_model=#{scenariosModel},scenarios_out=#{scenariosOut},
        scenarios_status=#{scenariosStatus},last_open_time=#{lastOpenTime} where id = #{id}
    </update>


    <delete id="delById">
        DELETE FROM t_scenarios_info WHERE id = #{id}
    </delete>


    <resultMap id="scanariosMap" type="Scenarios">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="scenariosName" column="scenarios_name"/>
        <result property="scenariosCategory" column="scenarios_category"/>
        <result property="scenariosDesc" column="scenarios_desc"/>
        <result property="scenariosModel" column="scenarios_model"/>
        <result property="scenariosOut" column="scenarios_out"/>
        <result property="scenariosStatus" column="scenarios_status"/>
        <result property="lastOpenTime" column="last_open_time"/>
    </resultMap>


</mapper>